# Data Sources and Lineage

## Market prices

`prices.csv` contains adjusted closes used for the listed-asset proxy portfolio. A sidecar `prices.csv.metadata.json` identifies whether the file was generated by the deterministic demo or downloaded from Yahoo Finance, when it was retrieved, and which series were requested.

Quality checks run on the raw frame before any imputation. Forward filling is limited to three rows and every affected ticker is reported in `imputation_audit` with raw missing, imputed, and remaining missing counts.

## Official RBA data

The `download-rba` command downloads two public Reserve Bank of Australia statistical tables:

| Dataset | RBA table | Curated series |
|---|---|---|
| AUD exchange rates | F11.1 | AUD/USD and AUD trade-weighted index |
| AUD zero-coupon curve | F17 | 0Y, 3M, 6M, 1Y, 2Y, 3Y, 5Y, 7Y, and 10Y yields |

The downloader writes a catalog containing the exact source URL, provider, UTC retrieval timestamp, row and series counts, observation range, and SHA-256 file hash. Downloaded data remains excluded from Git; the code and catalog construction are reproducible. Users must review source terms before redistributing raw observations.

## Control meanings

- `missing_required_tickers`: configured series absent from the raw file.
- `duplicate_dates`: duplicate raw observation dates.
- `missing_price_cells`: raw missing values before forward filling.
- `nonpositive_prices`: invalid prices at or below zero.
- `max_calendar_gap_days`: largest gap between observations.
- `data_age_days`: calendar days from the newest observation to execution date.
- `source_type`: `synthetic_demo`, `yahoo_finance`, or `unknown` for legacy/unlabelled files.

Synthetic data is intentionally stale relative to live reporting and can fail the freshness check. That is evidence that the control is active, not a pipeline failure.

## P&L attribution outputs

`desk_pnl_daily` contains the daily actual, hypothetical, and risk-theoretical P&L series used for PLA-style review. In the public demonstrator, actual P&L is deterministic and reproducible; in a production desk it would be sourced from an approved front-office ledger.

`pnl_attribution_summary` contains the model-validation decision metrics: correlation, distribution distance, mean absolute error, variance ratio, tail capture, and `pla_status`.

`pnl_factor_betas` records the OLS betas that map each configured portfolio asset to the selected market, FX, and rates factors. This table is the bridge between risk-theoretical P&L and the configured public risk factors.

## RFET and NMRF outputs

`rfet_observation_evidence` records the public-data frequency proxy for each risk factor: trailing observations, represented months, maximum and median gaps, proxy status, evidence type, and a clear flag that regulatory RFET is not passed by public closes alone.

`nmrf_stress_fallback` records the liquidity-horizon-scaled standalone stress loss that would be reviewed if a risk factor were deemed non-modellable. The table distinguishes public proxy triggers from capital calculation; `capital_measure` is false.

## APRA-aware boundary output

`prudential_evidence_map` maps each major project output to an APRA-style prudential theme. It records the relevant reference, project evidence, safe demonstrated capability, boundary statement, production evidence still needed, and `portfolio_claim_status`.

This table is intentionally conservative. It exists to show interview-ready prudential framing, not to assert compliance, approval, or production readiness.
